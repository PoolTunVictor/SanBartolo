version: '3.8'

services:
  backend:
    build: ./Backend
    ports:
      - "3000:3000"
    volumes:
      - ./Backend:/app

  frontend:
    build: ./Frontend
    ports:
      - "4400:80"
    depends_on:
      - backend
